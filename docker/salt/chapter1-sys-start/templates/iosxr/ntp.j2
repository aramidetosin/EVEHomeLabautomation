{% set servers = salt.pillar.get("ntp.servers") %}
{% set peers = salt.pillar.get("ntp.peers") %}

no ntp
{%- for server in servers %}
ntp server {{server}}
{%- endfor %}
{%- for peer in peers %}
ntp peer {{peer}}
{%- endfor %}
